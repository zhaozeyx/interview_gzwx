<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.demon.guangzhiweixin">

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity
      android:name=".BaseTimerActivity"
      android:label="@string/title_activity_base_timer"
      android:theme="@style/AppTheme.NoActionBar"></activity>
    <activity android:name=".MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".ThreadTimerActivity" />
    <activity android:name=".ThreadHandlerTimerActivity" />
    <activity android:name=".ThreadViewPostTimerActivity" />
    <activity android:name=".RxJavaTimerActivity" />
    <activity android:name=".CoroutineTimerActivity" />
  </application>

</manifest>